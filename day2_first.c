#include <stdio.h>

int main() {
    int num1 = 15, num2 = 20;

    if (num1 > num2) {
        printf("%d is larger\n", num1);
    } else {
        printf("%d is larger\n", num2);
    }

    return 0;
}
